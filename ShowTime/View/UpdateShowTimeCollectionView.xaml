<UserControl x:Class="ShowTime.View.UpdateShowTimeCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Height="Auto"  HorizontalAlignment="Stretch" Margin="10,10,10,10" Name="stackPanel1" VerticalAlignment="Top">
            <Button Content="Search" Height="23" Name="BtnSearch" Width="Auto" DockPanel.Dock="Right" />
            <TextBox Height="23" Name="textBox1" />
        </DockPanel>
        <ListView Name="listView1" Grid.Row="1" />
        <DockPanel Grid.Row="2" Height="Auto"  HorizontalAlignment="Stretch" Margin="10,10,10,10" VerticalAlignment="Top">
            <Button Content="Apply" Height="23" Name="BtnApply" Width="Auto" DockPanel.Dock="Right" />
        </DockPanel>
    </Grid>
</UserControl>
